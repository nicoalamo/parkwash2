<p id="notice"><%= notice %></p>

<h1>Washes</h1>

<table>
  <thead>
    <tr>
      <th>Creation date</th>
      <th>Discount amount</th>
      <th>Wash type</th>
      <th>Washer</th>
      <th>Vehicle</th>
      <th>Receipt</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @washes.each do |wash| %>
      <tr>
        <td><%= wash.creation_date %></td>
        <td><%= wash.discount_amount %></td>
        <td><%= wash.wash_type %></td>
        <td><%= wash.washer %></td>
        <td><%= wash.vehicle %></td>
        <td><%= wash.receipt %></td>
        <td><%= link_to 'Show', wash %></td>
        <td><%= link_to 'Edit', edit_wash_path(wash) %></td>
        <td><%= link_to 'Destroy', wash, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Wash', new_wash_path %>
