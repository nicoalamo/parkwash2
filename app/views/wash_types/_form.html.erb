<%= form_with(model: wash_type, local: true) do |form| %>
  <% if wash_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wash_type.errors.count, "error") %> prohibited this wash_type from being saved:</h2>

      <ul>
      <% wash_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
