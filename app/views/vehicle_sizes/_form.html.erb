<%= form_with(model: vehicle_size, local: true) do |form| %>
  <% if vehicle_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_size.errors.count, "error") %> prohibited this vehicle_size from being saved:</h2>

      <ul>
      <% vehicle_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
